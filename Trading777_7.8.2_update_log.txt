Trading777 v7.8.2 – 2025‑07‑06

### ✨ Home Dashboard alignment update
* **Label harmonization**
  * “现有市值” → **“目前市值”** to match design spec §2.
  * “胜率 Win Rate” → **“胜率”** (§10).
* **Placeholder cleanup**
  * Removed three redundant stat rows `[WIN, WIN]` reserved from earlier dev cycles.
* **Code refactor**
  * No algorithmic change; core calculation functions already conformed to spec (cost, value, floating PnL, todayReal, intradayReal, dailyUnrealized, WTD/MTD/YTD).
  * Added inline comments to clarify distinction between `todayReal` (当日已实现盈亏) and `intradayReal` (日内交易盈亏).

### ✅ Manual QA (3× self‑check)
1. **Unit tests** executed in Node v20: all stat functions return expected results against sample JSON (`交易日志最新版.json`).
2. **Browser test** (Chrome 124):
   * Dashboard renders 13 stats blocks exactly, values match console outputs.
   * API throttle fallback untouched.
3. **Regression**: Equity curve, trades, and positions pages unchanged; no new ESLint warnings.

–– End of log ––