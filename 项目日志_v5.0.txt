版本号：v5.0 正式版
更新时间：2025-06-30 23:48:18

【修改内容】
1. 方向与数量着色
   - 首页（index.html）交易记录表：BUY → green；SELL → red；SHORT → purple；COVER → blue。
     方向列与数量列同时着色。
   - 个股页面（stock.html）交易明细表同步实现相同着色规则。
   - 新增 .purple / .blue 配色样式至 css/style.css。

2. 新增“星期”列
   - 首页交易记录表新增“星期”列（英文缩写 Mon/Tue/…），并在客户端运行时根据交易日期自动转换。
   - 个股页面交易明细表原有 weekday 数值列升级为英文缩写显示，保持列顺序不变。

3. 个股页面顶部功能区居中
   - 动态计算统计卡片数量，运行时设置 gridTemplateColumns = repeat(N, 180px)，充分利用 .stats‑grid 的 justify-content:center，使 5 个卡片在宽屏下视觉居中。

4. 元数据
   - 所有页面 title 中版本号更新为 v5.0。

【未变更内容】
- 数据读取／写入逻辑（localStorage JSON 数据）保持不变。
- FIFO 盈亏与仓位计算算法未做修改。
- 导入 / 导出功能、编辑 / 删除交易、整体文件结构与打包脚本均保持原先实现。
- 其它页面（trades.html、持仓视图、顶部时区时钟等）视觉与交互保持与 v3.3.5 一致。

已完成本地三轮自检：
✓ 页面正常加载，无白屏或 404；
✓ 新增列与着色规则与测试数据匹配；
✓ 新旧数据兼容，个股页面交易记录无丢失。