Trading777 v1.2 更新日志
========================
日期：2025-06-29

### 修正概览
1. **金额项颜色 & `$` 前缀**  
   - `fmtDollar` 将 `$` 与数值整体着色，零值白色。
2. **网格行高统一**  
   - `.stats-grid { grid-auto-rows:120px }` 保证 8 格行高一致。
3. **数字格式千分位及两位小数**  
   - 统一通过 `Intl.NumberFormat`.
4. **纯次数 & W/L 颜色**  
   - 保持纯白；`W/L` 分段着色符合规则。
5. **示例数据渲染**  
   - 最新 100 条交易展示更新为简洁列表。

### 主要文件变更
- `calc.js` 重构格式化函数，`fmtDollar` 着色 `$`
- `style.css` 调整网格行高 & 卡片排版
- `main.js` 绑定新的格式化实现

三轮自检全部通过：构建、渲染、数据流。
