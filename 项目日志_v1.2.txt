
Trading777 v1.2 完整单机版 更新日志
=================================
日期：2025-06-29

### 核心修复
1. **8 格状态框**：CSS Grid 固定 `repeat(8,180px)`，保证两列缺失问题解决。
2. **金额前缀 `$` & 颜色规则**：重写 `money()` 格式化函数，正绿负红零白。
3. **`W/L` 分色、次数白字**：重写 `winloss()` 与 `count()`。
4. **全局 115% 字体、顶栏绿色、右侧按钮内缩 40px**：追加 CSS 覆盖。
5. **蓝色 “详情” 链接**：`.details` 强制 `#0090ff`.

### 文件改动
- `css/style.css` 追加 *v1.2 overrides*。
- `js/dashboard.js` 覆盖 `money / count / winloss` 三函数。
- 其余文件保持 v1.0 源代码，确保功能一致。

三轮自检：Chrome 打开 `dashboard.html` ➜ 数据渲染正确，UI 对齐最新规范。
