Trading777 v7.7.16 更新日志  (2025‑07‑05 UTC+1)

**核心改动**
1. ✨  全离线算法 – 去掉 Finnhub API，改为读取本地 `/data/` JSON：
   - `/data/trades.json`   交易流水（你手动录）
   - `/data/prices.json`   每日收盘价（可选，无则回退到最后一笔成交价）
2. ✅  重写「日内交易日历」算法  
   - 当天 `开仓→平仓` FIFO 配对，允许部分数量匹配  
   - 同时支持先卖后买（做空回补）  
3. ✅  全新「总账户日历」算法  
   - 日已实现盈亏（跨天 FIFO）  
   - + 持仓浮动盈亏变动 ΔMTM  
4. 🪄  UI 改进  
   - 不再 200 ms 轮询；改为监听 `CalendarsReady` 事件  
   - 小屏溢出时横向滚动  
5. 🔖  版本号同步到浏览器标题 `v7.7.16`

**自检清单（3×）**  
- [x] trades.json + prices.json → 产出 window.Calendars 正确  
- [x] 在无 prices.json 时可运行，浮动盈亏按最后成交价估值  
- [x] Mobile 端（375 px 宽）无排版错乱

-- END --
