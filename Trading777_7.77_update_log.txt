Trading777 v7.77 更新日志 - 2025-07-09
修复内容:
1. 首页 13 个统计模块无法实时更新的问题:
   - 在 js/dashboard.js 的 updatePriceCells() 末尾新增 renderStats() 调用, 保证每次实时报价刷新后同步刷新统计面板。
2. 修复 recalcPositions() 闭仓标记逻辑:
   - 将 t.closed 条件从 `realized !== 0` 修改为 `remaining === 0`，保证即便盈亏为 0 也正确算作已平仓，确保当日已实现盈亏、胜率等统计准确。
3. 浏览器缓存失效处理:
   - index.html <title> 更新到 Trading777 v7.77。
   - 为所有 css/js 资源追加 '?v=7.77' 查询串，强制浏览器加载新版本脚本，避免旧代码缓存导致的数据显示错误。
4. 代码自检:
   - 本地跑通：页面加载无报错，报价与统计均同步更新。
   - 文件结构保持不变，可直接上传 GitHub。

未变动内容:
- 持仓管理、交易记录、图表等其他模块保持原样。
- API Key 读取及价格拉取逻辑保持不变。
