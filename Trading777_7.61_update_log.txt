项目日志（版本 7.61） - 更新于 2025-07-08 21:48

📌 本次更新详细记录：
1. **修复实时价格 API 失效（Finnhub）**
   - `js/services/priceService.js`
   - 读取 `KEY.txt` 时扩大正则表达式范围，兼容小写字母并自动回退到内置 demo token，以避免因 KEY 格式差异导致 token 为空而无法请求。
   - 若 KEY.txt 中找不到 FINNHUB token，将自动使用 `d19cvm9r01qmm7tudrk0d19cvm9r01qmm7tudrkg`。

2. **版本号提升**
   - 新增 `Trading777_7.61_update_log.txt`。

🆗 未动部分：
- 页面布局、样式与其他 JS 模块保持 7.60 版本原样。
- 所有历史算法、缓存与数据结构不变。
