Trading777 v1.3 修订日志
========================
日期：2025-06-29

### 修复内容
- **统计区空白**：确保 HTML 含 8 盒，JS 正确挂载 & 无模块路径错误。
- **金额格式**：统一 `$ 123,456.78`；正绿、负红、零白。
- **W/L 分色**：`W` 绿色，`L` 红色。
- **次数项白字**：当日 / 累计交易次数。
- **CSS Grid**：`repeat(8,180px)` 固宽；全局字体 115 %。
- **“详情” 链接** 蓝色。

### 用法
1. 解压后直接双击 `index.html` 离线可用。  
2. 如需接 Supabase / 后端，只需替换 `positions`、`trades` 获取方式即可，统计渲染逻辑已独立封装。
