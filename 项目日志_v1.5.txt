Trading777 v1.5 更新日志
========================
日期：2025-06-30

### 新增
1. **顶栏世界时钟** — 纽约 / 伦敦 / 上海，每秒实时更新。
2. **股票详情页** `stock.html` — 点击持仓或交易表里的 “详情” 自动带 `?symbol=` 跳转并展示该股票交易记录。
   - 离线版通过 `localStorage` 共享 demo 数据，可自行替换接后端。
3. 保持 8 格统计算法符合需求，不影响既有功能。
