{
  "name": "trading-app-monorepo",
  "private": true,
  "packageManager": "pnpm@8.10.5",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:core": "vitest run --coverage"
  },
  "devDependencies": {
    "turbo": "^1.13.0",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  }
}
